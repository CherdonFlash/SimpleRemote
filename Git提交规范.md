# Git提交规范

本工程从本文件加入仓库后的下一次提交开始，统一采用[约定式提交（Conventional Commits 1.0.0）](https://www.conventionalcommits.org/zh-hans/v1.0.0/)格式。

除Git要求的固定关键字、代码标识符、器件型号和文件名外，提交标题、正文和脚注说明必须使用中文。所有参与者及自动化工具在本工程中创建Git提交时都必须遵守本规范。

## 1. 强制格式

```text
<类型>(<范围>)<可选的!>: <中文描述>

<可选的中文正文>

<可选的脚注>
```

最简示例：

```text
docs(readme): 更新V1.1硬件架构说明
```

完整示例：

```text
feat(mcu): 增加CW2015电量计驱动

通过I2C2读取电池电压、SOC和剩余运行时间，移除旧版PB1电池ADC采集逻辑。

Refs: #12
```

## 2. 标题规则

每个提交标题必须同时满足以下要求：

1. 必须使用本规范允许的英文小写类型。
2. 必须填写英文小写范围，并使用圆括号包围。
3. 类型/范围后必须使用英文冒号和一个空格，即`): `。
4. 冒号后的描述必须使用中文，器件型号、接口名和文件名可以保留原文。
5. 描述应直接说明本次提交完成了什么，不使用“修改一下”“更新文件”等模糊措辞。
6. 标题末尾不使用句号、感叹号等标点。
7. 标题建议控制在72个字符以内；确需补充的信息写入正文。
8. 一个提交只处理一个主要目的，不能把互不相关的修改放入同一提交。

## 3. 允许的类型

| 类型 | 用途 | 示例 |
| --- | --- | --- |
| `feat` | 增加用户可见功能、硬件功能或接口能力 | `feat(pcb): 增加CW2015电量计电路` |
| `fix` | 修复软件、原理图、PCB或结构设计缺陷 | `fix(power): 修正双电源切换控制网络` |
| `docs` | 只修改文档、注释或说明 | `docs(readme): 更新电量计架构图` |
| `refactor` | 重构实现，但不改变对外功能 | `refactor(mcu): 拆分电池管理模块` |
| `perf` | 提升性能、效率、功耗或资源占用 | `perf(power): 降低电池供电静态功耗` |
| `test` | 增加或修改测试、校核脚本 | `test(pcb): 增加网表一致性检查` |
| `build` | 修改编译系统、工具链或依赖 | `build(keil): 更新目标芯片编译配置` |
| `ci` | 修改持续集成或自动化检查 | `ci(git): 增加提交信息检查流程` |
| `chore` | 文件归档、版本整理等维护工作 | `chore(hardware): 归档新版遥控器V1.1设计文件` |
| `style` | 仅调整代码格式，不改变逻辑 | `style(mcu): 统一驱动代码缩进格式` |
| `revert` | 撤销以前的提交 | `revert(pcb): 撤销电源层布线调整` |

不得自行创造`hardware`、`update`、`modify`等新类型。硬件只是修改范围，应写成`feat(hardware)`、`fix(hardware)`或`chore(hardware)`。

## 4. 本工程允许的范围

| 范围 | 对应内容 |
| --- | --- |
| `mcu` | STM32固件通用修改 |
| `battery` | 电池检测、电量计及电池保护 |
| `power` | 充电、稳压、升压和双电源切换 |
| `radio` | NRF24L01无线通信 |
| `oled` | OLED显示及I2C1通信 |
| `pcb` | PCB布局、布线、层叠和封装 |
| `schematic` | 原理图和网表 |
| `hardware` | 同时涉及多个电子硬件模块的修改或归档 |
| `mechanical` | 外壳、装配体和SolidWorks模型 |
| `docs` | 多份通用文档 |
| `readme` | README说明 |
| `git` | Git配置、提交规范和仓库策略 |
| `build` | Keil工程、编译脚本和工具链 |
| `release` | 版本发布与发布说明 |

如果修改只涉及单一模块，应使用最具体的范围；同时涉及多个硬件模块时才使用`hardware`。需要新增范围时，应先修改本文件，再在后续提交中使用新范围。

## 5. 正文规则

简单修改可以省略正文。以下情况必须填写正文：

- 修改原因无法通过标题完整说明。
- 涉及硬件设计取舍、兼容性、功耗或安全影响。
- 修改了多个相互关联的文件。
- 需要记录验证方式、测试结果或已知限制。

标题和正文之间必须空一行。正文应优先说明“为什么修改”“如何修改”“验证结果”，避免简单重复标题。

示例：

```text
fix(power): 修正电池模式下Q2栅极下拉路径

USB移除后通过独立电阻为Q2栅极提供确定的低电平，避免依赖输入节点漏电流。
已核对原理图网表和PCB焊盘网络。
```

## 6. 破坏性变更

不兼容旧硬件、旧固件、通信协议或数据格式的修改必须标记为破坏性变更。

可以在范围后增加`!`：

```text
feat(radio)!: 调整无线数据帧中的电量字段
```

同时应在脚注中使用规范要求的固定关键字`BREAKING CHANGE:`，其说明使用中文：

```text
feat(radio)!: 调整无线数据帧中的电量字段

BREAKING CHANGE: 接收端必须同步升级，否则无法正确解析新的电量数据。
```

## 7. 问题关联和脚注

脚注与正文之间必须空一行。常用写法：

```text
Refs: #12
Closes: #15
Reviewed-by: 姓名
```

固定字段名可以使用英文，字段后的说明必须使用中文。

## 8. 本工程示例

```text
feat(battery): 增加CW2015电量计驱动
feat(pcb): 将电量计连接到PB10和PB11
fix(schematic): 修正电量计I2C网络标签
perf(power): 降低关机状态静态电流
docs(readme): 更新V1.1功能架构图
chore(hardware): 归档新版遥控器V1.1设计文件
test(mechanical): 增加外壳装配干涉检查
```

## 9. 禁止示例

```text
update files
修改代码
hardware: archive V1.1 design
feat: 增加电量计
feat(PCB): 增加电量计
docs(readme): update architecture
```

禁止原因依次为：缺少类型和范围、描述模糊、类型不在允许列表、缺少范围、范围不是小写、描述未使用中文。

## 10. 提交前检查

每次提交前必须依次完成：

1. 使用`git status --short`确认修改范围。
2. 使用`git diff --cached`核对暂存内容。
3. 确认没有密钥、账号、临时文件、编译缓存或无关修改。
4. 执行与修改相关的编译、测试、ERC、DRC或静态检查。
5. 按本规范编写中文提交描述。
6. 提交后检查`git log -1 --format=%B`。
7. 推送后确认本地提交与远端目标分支一致。

命令示例：

```powershell
git add README.md Git提交规范.md
git diff --cached
git commit -m "docs(git): 新增约定式提交规范"
git push origin main
```

## 11. 不符合规范时的处理

- 尚未推送：必须使用`git commit --amend`修正提交信息后再推送。
- 已经推送：不得擅自强制推送或改写公共历史；需要修正时必须先获得项目负责人明确同意。
- 合并提交、自动回滚提交等由Git平台生成的信息可以保留平台默认格式。
- 本规范生效前的历史提交不要求追溯修改。

## 12. 自动检查建议

当前首先以本文件作为强制流程规范。后续可以增加commit-msg Hook或CI，并使用commitlint的`@commitlint/config-conventional`规则自动拒绝不合规提交。若自动检查规则与本文件冲突，应以本文件中的项目专用类型、范围和中文描述要求为准，并同步修正规则配置。
